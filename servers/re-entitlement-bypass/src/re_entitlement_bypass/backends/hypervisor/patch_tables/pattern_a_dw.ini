[Config]
; P3R (Persona 3 Reload) — Denuvo is confirmed present in this build
; (per data/wire_sigs/p3r.json: DODENUVO markers + Denuvo-style custom
; sections + ATAPI dispatch table + 1.95M CPUID + 2.04M INT_3 + 36 VMCALL).
;
; BUT: per SOW-P §P.1, the Denuvo ATD is a contractual carve-out —
; the engagement does NOT include Denuvo bypass for P3R.
;
; This patch table is therefore EMPTY — no Denuvo deploy, no patches.
; The orchestrator's per-target manifest (data/targets.json) does NOT
; include the `denuvo` layer for P3R.
;
; Kept here for reference + as the template for a future engagement
; that does include Denuvo bypass for P3R (with a new SOW amendment).

Targets=
AutoLoadHV=false
GoRevertMsg=false
Capcom=false

[LoadDlls]
; (empty)

[Sections]
; (empty)

[Patches:P3R.exe]
; (empty — Denuvo carve-out)

[Hashes]
; (empty)
